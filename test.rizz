# test.rizz

nocap myName = "jeff";
huh myAge = 28;

finna sayHello(name, age) {
    yap(`hello ${name}, you are ${age}`);

    noway (age < 18) {
        yap(`you're a minor`);
    } unless noway (age >= 18 && age < 65) {
        yap(`you're an adult`);
    } unless (age > 65) {
        yap(`you're a senior`);
    }
}

sayHello(myName, myAge);

huh i = 0;
sigma (i < 5) {
    yap(i);
    i++;
}